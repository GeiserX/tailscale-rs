{
  "schema": "parity-audit/v1",
  "auditedPrs": [
    {
      "url": "https://github.com/GeiserX/tailscale-rs/pull/472",
      "verdict": "gaps",
      "notes": "Squash 8e2de14 on main, audited against github.com/tailscale/tailscale @ e2ed432399c9b0fda7aa14e9eb27784d2d893c55 (the pin PORTING.md documents), cloned shallow. The pong itself is faithful: Pong{TxID, Src: 127.3.3.40:<arrival region>} sealed to the ping's sender disco key, addressed to the DERP source node key, sent on the region the ping arrived on, nothing learned, region id truncated to u16 as Go does; a Geneve-framed disco frame over DERP goes to the dataplane on both sides. Five divergences remain around it, none inside the sealed frame: three in the reply path this PR added (DERP-only posture, blocking/teardown on a failed pong send, and a pong admission check that binds the sender disco key to the DERP source node key where upstream checks the two independently), two the PR left in the function it rewrote."
    }
  ],
  "gate": {
    "ran": true,
    "pass": true,
    "detail": "bin/check is not readable on this box (cargo deny stands red on any branch over the aws-lc-rs/aws-lc-sys bans and a licence rule, and --all-features does not build ts_cli_util), so the stages it gates were run directly on main at 8e2de14 with CARGO_BUILD_JOBS=4/RUST_TEST_THREADS=4: clippy --lib and clippy --bins/--tests/--benches/--examples over --workspace --exclude ts_cli_util with -D warnings, both clean; cargo nextest run -p geiserx_ts_control -p geiserx_ts_runtime --all-features --no-fail-fast, 956 passed / 3 skipped in 75s; cargo build --workspace --exclude ts_cli_util --all-targets --all-features, clean. The 3 skipped are the socket-bound tests this box cannot run (serve_path_proxy_replays_consumed_head_to_backend and proxy_with_prefix_writes_prefix_before_bidi_copy hang on a dropped SYN, probe_stun_servers_once_sends_binding_request needs outbound UDP); they were filtered out rather than left to hang. A separate run of cargo nextest run -p geiserx_ts_magicsock -p geiserx_ts_runtime --all-features --no-fail-fast on the same commit ran 764 tests, 757 passed and 7 failed; all 7 are geiserx_ts_magicsock sock::tests that time out on a real loopback UDP socket this box drops on the wire, and every test #472 added or rewrote passed (relayed_ping_is_ponged_without_learning_a_path, relayed_ping_without_verifier_is_not_ponged, relayed_ping_is_ponged_to_derp_source, relayed_ping_from_node_key_not_a_peer_is_not_ponged, geneve_framed_relayed_ping_is_not_ponged). The admission finding was checked separately on main at 225ea44 with the same bounds: clippy (both passes) clean; cargo nextest run -p geiserx_ts_magicsock -p geiserx_ts_runtime --all-features --no-fail-fast, 760 run, 753 passed, 7 failed, all seven ts_magicsock sock::tests timing out on a real loopback UDP socket this box blocks, with the 28 tests #472 added or rewrote passing; cargo build --workspace --exclude ts_cli_util --all-targets --all-features, clean."
  },
  "findings": [
    {
      "prUrl": "https://github.com/GeiserX/tailscale-rs/pull/472",
      "title": "A DERP-relayed ping is only answered when the direct UDP socket bound",
      "detail": "Upstream answers a disco ping received over DERP from the DERP reader itself: processDERPReadResult hands the frame to handleDiscoMessage -> handlePingLocked, which replies with sendDiscoMessage to the DERP address. Nothing on that path depends on the UDP socket being usable — when the UDP bind fails upstream substitutes blockForeverConn and the Conn, its DERP reader and its disco handling keep running. Here the pong is produced only inside MagicSock, and the DERP region runner reaches MagicSock only through Multiderp's direct_sock handle, which DirectManager installs (multiderp::SetDirectSock) only after a successful bind_underlay_addr; on bind failure DirectManager returns early with sock: None and never sends it. In that DERP-only posture — exactly the case this PR set out to fix, and the case where the peer has no other way to reach us — every relayed ping falls through to the dataplane as junk and the peer's `tailscale ping` times out just as it did before the change. The same hole is open as a startup race on a healthy node, between the first DERP connection and SetDirectSock being handled.",
      "upstream": "wgengine/magicsock/derp.go @ e2ed432399c9b0fda7aa14e9eb27784d2d893c55 (processDERPReadResult; with wgengine/magicsock/magicsock.go handlePingLocked and wgengine/magicsock/blockforever_conn.go)",
      "severity": "major"
    },
    {
      "prUrl": "https://github.com/GeiserX/tailscale-rs/pull/472",
      "title": "A failed pong send tears down the whole region's DERP connection, and the send blocks the DERP reader",
      "detail": "Upstream never sends disco from the DERP read path: handlePingLocked does `go c.sendDiscoMessage(...)`, which reaches sendAddr, which pushes a derpWriteRequest onto the region's write channel with a three-attempt, drop-oldest policy and returns errDropDerpPacket rather than blocking; the actual write happens in runDerpWriter, where a derp.Send error is logged (metricSendDERPError) and the writer loop continues. A pong that cannot be sent costs upstream exactly that pong. The merged code sends the reply inline in the region read loop — `client.send_one(to, &frame).await?` in run_derp_once — so a slow DERP write stalls inbound delivery for every peer on that region behind it, and any send error propagates out of run_derp_once, dropping the region's connection and putting it into the reconnect backoff. That teardown is reachable by any netmap peer simply by pinging this node over DERP.",
      "upstream": "wgengine/magicsock/magicsock.go @ e2ed432399c9b0fda7aa14e9eb27784d2d893c55 (handlePingLocked reply, sendAddr; and wgengine/magicsock/derp.go runDerpWriter)",
      "severity": "major"
    },
    {
      "prUrl": "https://github.com/GeiserX/tailscale-rs/pull/472",
      "title": "DERP pong admission is stricter than upstream: sender disco key must be bound to the DERP source node key",
      "detail": "Upstream: handleDiscoMessage admits a frame when c.peerMap.knownPeerDiscoKey(sender) is true for ANY peer; handlePingLocked with isDerp then sets numNodes = 1 whenever c.peerMap.endpointForNodeKey(derpNodeSrc) succeeds, and pongs. The two checks are independent: a known disco key plus a known DERP source node key is enough, and unambiguousNodeKeyOfPingLocked's result is only used to update epAddr mappings on the non-DERP path. Upstream explicitly tolerates one disco key mapping to several node keys (comment on numNodes: node sharing, users switching accounts). Merged: MagicSock::answer_derp_ping calls verify(sender, Some(src.node_key)), and the runtime verifier (ts_runtime/src/direct.rs verify_binding) resolves the disco key to ONE peer via peer_by_known_disco_key and requires that peer's node key to equal the DERP source. A netmap where two node keys share one disco key (multi-profile machine, node sharing) gets a pong only for the peer the lookup happens to return; upstream pongs both. The PR's DECISIONS acknowledges the extra refusal but scopes it to a relayed-ping attack; the legitimate shared-disco-key case is not covered by the tests. Upstream also names the relation a set outright (peerMap.nodesOfDisco: 'Multiple nodes may share the same disco key in the case of node sharing and users switching accounts'), while PeerDb::peer_by_known_disco_key resolves through a HashMap<DiscoPublicKey, PeerId> index (disco_idx), one peer per disco key. The losing peer's `tailscale ping` over DERP times out, the symptom the PR set out to remove. The shape is inherited from the direct arm's verifier, but the PR ports the DERP arm onto it.",
      "upstream": "wgengine/magicsock/magicsock.go (handleDiscoMessage knownPeerDiscoKey gate; handlePingLocked isDerp/numNodes; unambiguousNodeKeyOfPingLocked) @ e2ed432399c9b0fda7aa14e9eb27784d2d893c55",
      "severity": "minor"
    },
    {
      "prUrl": "https://github.com/GeiserX/tailscale-rs/pull/472",
      "title": "A disco Pong relayed over DERP is still dropped, against the processDERPReadResult behaviour the diff cites",
      "detail": "The PR cites processDERPReadResult for `what reaches the disco handler`, and upstream hands every non-Geneve disco frame over DERP to handleDiscoMessage — including a Pong, which is dispatched to endpoint.handlePongConnLocked. There the DERP case is explicit and live: the sentPing entry is removed, latency is computed from it and the CLI ping callback is fired (only the endpointState/bestAddr updates are guarded by !isDerp). That is how a node that pinged a peer over DERP completes its own `tailscale ping`. handle_relayed_disco still drops a relayed Pong, and the rewritten doc comment states the upstream rationale as `a Pong has no meaning without a matching ping we sent on this path` — true of this tree today only because nothing here ever sends a disco ping over DERP (Multiderp::send_disco has no caller in the workspace), not because upstream behaves that way. The reception side of the DERP ping/pong exchange is therefore half-ported, and the claim recorded in the code will mislead whoever ports the sending half.",
      "upstream": "wgengine/magicsock/endpoint.go @ e2ed432399c9b0fda7aa14e9eb27784d2d893c55 (handlePongConnLocked; reached via wgengine/magicsock/derp.go processDERPReadResult)",
      "severity": "minor"
    },
    {
      "prUrl": "https://github.com/GeiserX/tailscale-rs/pull/472",
      "title": "CallMeMaybe over DERP is still admitted on disco-key membership alone, not bound to the DERP source node key",
      "detail": "Upstream's CallMeMaybe/CallMeMaybeVia arm refuses the frame unless the DERP source node key is itself a peer (`nodeKey := derpNodeSrc; ep, ok := c.peerMap.endpointForNodeKey(nodeKey)`), and then unless the sender's disco key belongs to that endpoint (`epDisco, knownKey := ep.checkAndUpdateDiscoKey(di.discoKey)`; `!knownKey` logs `CallMeMaybe from peer via DERP whose netmap discokey != disco source` and returns). The endpoints are applied to that node's endpoint. Here call_me_maybe_sender_allowed still calls the verifier with None, which verify_binding treats as membership-only — any peer's disco key that is in the netmap is accepted, and the endpoints are attributed by disco key with no reference to who the DERP server says sent the frame. This PR threads exactly the missing evidence (DerpSource) into this function and uses it only on the Ping arm. The gap is pre-existing rather than introduced, but it now sits one argument away from being closed: a peer that captured another peer's CallMeMaybe can replay it to us over DERP and have it acted on, which upstream refuses.",
      "upstream": "wgengine/magicsock/magicsock.go @ e2ed432399c9b0fda7aa14e9eb27784d2d893c55 (handleDiscoMessage, the *disco.CallMeMaybe / *disco.CallMeMaybeVia case)",
      "severity": "minor"
    }
  ]
}
